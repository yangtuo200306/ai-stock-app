# v1.9.2 流式响应（Streaming）计划

## 版本目标

LLM 回答逐字输出，用户不用干等 3-5 秒。不改架构、不改数据源。

## 改动范围

| 层 | 文件 | 改动 |
|----|------|------|
| 后端 | `backend/app/services/llm_client.py` | 新增 `ask_llm_stream()` 函数 |
| 后端 | `backend/app/api/ask.py` | 新增 `POST /api/ask/stream` 端点 |
| 前端 | `mobile-app/src/api/client.ts` | 新增 `apiPostStream()` 函数 |
| 前端 | `mobile-app/src/stores/askStore.ts` | 新增流式处理逻辑 |
| 前端 | `mobile-app/src/screens/AskScreen.tsx` | MessageBubble 流式渲染 |

## 后端改动

### 1. `llm_client.py` — 新增 `ask_llm_stream()`

```python
def ask_llm_stream(
    stock_code: str,
    stock_name: str,
    price: float,
    change_pct: float,
    trend: str,
    action: str,
    score: int,
    summary: str,
    risks: list[str],
    indicators: dict,
    question: str,
    conversation_messages: list[dict] | None = None,
) -> Generator[str, None, None]:
    """流式调用 LLM，逐 chunk yield 文本。"""
    api_key, base_url, model = _get_llm_config()
    url = f"{base_url.rstrip('/')}/chat/completions"
    prompt = _build_prompt(...)  # 复用现有函数

    messages = [{"role": "system", "content": "..."}]
    # 拼接 conversation_messages...

    payload = {
        "model": model,
        "messages": messages,
        "temperature": 0.7,
        "max_tokens": 1024,
        "stream": True,  # 关键
    }

    headers = {"Authorization": f"Bearer {api_key}", "Content-Type": "application/json"}

    resp = requests.post(url, json=payload, headers=headers, stream=True)
    for line in resp.iter_lines():
        if line:
            line = line.decode("utf-8")
            if line.startswith("data: "):
                data_str = line[6:]
                if data_str == "[DONE]":
                    break
                chunk = json.loads(data_str)
                delta = chunk["choices"][0]["delta"].get("content", "")
                if delta:
                    yield delta
```

### 2. `ask.py` — 新增 `POST /api/ask/stream`

```python
@router.post("/ask/stream")
async def ask_stream(
    ask: AskRequest,
    authorization: str = Header(...),
):
    user_id = verify_token(authorization)

    # 1. 爬数据、算指标、算评分（和现有逻辑完全一样）
    report = _build_report(stock_code)

    # 2. 流式调用 LLM
    return StreamingResponse(
        ask_llm_stream(
            stock_code=stock_code,
            stock_name=report["stock_name"],
            ...
        ),
        media_type="text/event-stream",
        headers={
            "X-Result-Data": json.dumps({
                "stock_name": report["stock_name"],
                "stock_code": stock_code,
                "score": report["score"],
                "trend": report["trend"],
                "action": report["action"],
                "indicators": report["indicators"],
            }),
            "Cache-Control": "no-cache",
        },
    )
```

**关键设计**：流式结束后，前端通过响应头 `X-Result-Data` 拿到完整的指标数据，用于展示结果卡片。

## 前端改动

### 1. `client.ts` — 新增 `apiPostStream()`

```typescript
async function apiPostStream(
  path: string,
  body: object,
  onChunk: (text: string) => void,
  onDone: (headers: Headers) => void,
  onError: (error: string) => void,
): Promise<void> {
  const token = storage.getString("token");
  const response = await fetch(`${API_BASE}${path}`, {
    method: "POST",
    headers: {
      "Content-Type": "application/json",
      Authorization: `Bearer ${token}`,
    },
    body: JSON.stringify(body),
  });

  if (!response.ok) {
    const err = await response.json();
    onError(err.message || "请求失败");
    return;
  }

  const reader = response.body?.getReader();
  const decoder = new TextDecoder();

  while (reader) {
    const { done, value } = await reader.read();
    if (done) break;
    const text = decoder.decode(value, { stream: true });
    onChunk(text);
  }

  onDone(response.headers);
}
```

### 2. `askStore.ts` — 新增流式处理

```typescript
handleAskStream: async (question: string) => {
  set({ isLoading: true, error: null });

  // 添加用户消息
  const userMsg = { id: uuid(), role: "user", content: question };
  set(state => ({ messages: [...state.messages, userMsg] }));

  // 添加占位 assistant 消息
  const assistantMsg = { id: uuid(), role: "assistant", content: "" };
  set(state => ({ messages: [...state.messages, assistantMsg] }));

  try {
    await apiPostStream(
      "/api/ask/stream",
      { stock_code: stockCode, question },
      (chunk) => {
        // 逐 chunk 追加到最后一条消息
        set(state => {
          const msgs = [...state.messages];
          const last = msgs[msgs.length - 1];
          msgs[msgs.length - 1] = { ...last, content: last.content + chunk };
          return { messages: msgs };
        });
      },
      (headers) => {
        // 流结束，解析 X-Result-Data
        const resultData = headers.get("X-Result-Data");
        if (resultData) {
          set({ latestResult: JSON.parse(resultData) });
        }
        set({ isLoading: false });
      },
      (error) => {
        set({ error, isLoading: false });
      },
    );
  } catch (e) {
    set({ error: e.message, isLoading: false });
  }
}
```

### 3. `AskScreen.tsx` — MessageBubble 流式光标

MessageBubble 组件：如果 `role === "assistant"` 且内容末尾没有完整句子结束符，显示闪烁光标。

```tsx
// 在 MessageBubble 内部
<Text>
  {content}
  {isStreaming && <Text style={styles.cursor}>|</Text>}
</Text>
```

## 不改范围

- 不改变现有 `POST /api/ask` 接口（保留非流式降级）
- 不改变消息数据结构
- 不改变导航
- 不改变数据库

## 验收清单

| 验收项 | 状态 |
|--------|------|
| AI 回答逐字输出，不用等全部生成完 | ✅ |
| 流式输出时消息末尾有光标闪烁 | ✅ |
| 流式结束后光标消失，结果卡片正常展示 | ✅ |
| 非流式降级路径（`POST /api/ask`）仍可用 | ✅ |
| 多轮追问在流式模式下正常 | ✅ |
