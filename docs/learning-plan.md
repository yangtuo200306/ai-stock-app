# AI Stock App 学习计划

## 1. 学习目标

本项目的主要目标不是一次性做出完整产品，而是通过参考 `stock-analysis-reference` 原项目，逐步学习并实践一个手机股票分析 APP 的完整开发流程。

核心目标：

- 学会从原项目中提取架构思路，而不是直接照搬代码。
- 学会设计自己的项目结构。
- 学会后端 API、数据库、任务队列、移动端页面、接口联调。
- 学会用 Git 管理自己的项目。
- 学会和 AI 助手协作：先研究、再计划、再执行、再审查。
- 最终让 APP 从最小闭环开始，逐步演进成更完整的股票分析工具。

## 2. 项目边界

当前工作区结构：

```text
d:/ai-stock-analysis/
  stock-analysis-reference/   # 原项目，只读参考
  ai-stock-app/               # 自己开发的新项目
    backend/                  # 后端服务
    mobile-app/               # 手机 APP
    docs/                     # 学习与项目文档
```

边界原则：

- `stock-analysis-reference` 只作为教材阅读，不修改、不提交、不作为自己的项目维护。
- `ai-stock-app` 是自己的项目，后续只在这里初始化 Git 和开发功能。
- 需要参考原项目时，优先学习思想和结构，再在自己的项目中重新实现。
- 不一开始追求原项目全部功能，先完成最小闭环。

## 3. 第一版 APP 目标

第一版目标是完成一个最小可用闭环：

```text
添加股票
  ↓
点击分析
  ↓
后端创建任务
  ↓
APP 查询任务状态
  ↓
生成并保存报告
  ↓
APP 查看报告详情
  ↓
APP 查看历史报告
```

第一版不追求真实行情和复杂 AI 分析，可以先使用 mock 数据生成报告。

## 4. 第一版页面

第一版计划 5 个页面：

| 页面 | 功能 |
|---|---|
| 首页 / 自选股页 | 添加股票、删除股票、展示自选股、触发分析 |
| 分析任务页 | 展示任务状态、进度、失败原因、完成后跳转报告 |
| 报告详情页 | 展示评分、建议、趋势、摘要、风险、技术指标 |
| 历史报告页 | 展示历史报告列表，点击进入详情 |
| 设置页 | 配置后端地址，测试连接 |

## 5. 第一版后端接口

| 接口 | 作用 |
|---|---|
| `GET /api/health` | 健康检查 |
| `GET /api/stocks` | 获取自选股 |
| `POST /api/stocks` | 添加自选股 |
| `DELETE /api/stocks/{code}` | 删除自选股 |
| `POST /api/analysis` | 创建分析任务 |
| `GET /api/analysis/{task_id}` | 查询任务状态 |
| `GET /api/reports` | 获取历史报告列表 |
| `GET /api/reports/{id}` | 获取报告详情 |

## 6. 第一版数据库

第一版使用 SQLite，建议三张表：

### stocks

保存自选股。

| 字段 | 说明 |
|---|---|
| id | 主键 |
| code | 股票代码 |
| name | 股票名称 |
| created_at | 添加时间 |

### analysis_tasks

保存分析任务状态。

| 字段 | 说明 |
|---|---|
| id | 主键 |
| task_id | 任务 ID |
| stock_code | 股票代码 |
| status | pending / processing / completed / failed |
| progress | 进度 |
| message | 当前消息 |
| report_id | 报告 ID |
| error | 错误信息 |
| created_at | 创建时间 |
| updated_at | 更新时间 |

### reports

保存分析报告。

| 字段 | 说明 |
|---|---|
| id | 主键 |
| stock_code | 股票代码 |
| stock_name | 股票名称 |
| price | 当前价格 |
| score | 评分 |
| action | 操作建议 |
| trend | 趋势判断 |
| summary | 分析摘要 |
| risks_json | 风险 JSON |
| indicators_json | 技术指标 JSON |
| created_at | 创建时间 |

## 7. 第一版 mock 分析规则

第一版可以先模拟报告：

| 分数 | 建议 | 趋势 |
|---|---|---|
| 70-100 | 买入 | 看多 |
| 40-69 | 观望 | 震荡 |
| 0-39 | 卖出 | 看空 |

报告字段：

- 股票代码
- 股票名称
- 当前价格
- 评分
- 操作建议
- 趋势判断
- 分析摘要
- 风险提示
- 技术指标
- 生成时间

## 8. 学习路线

### 阶段 1：理解原项目最小闭环

重点阅读：

- `server.py`
- `api/app.py`
- `api/v1/router.py`
- `api/v1/endpoints/analysis.py`
- `src/services/task_queue.py`
- `src/services/analysis_service.py`
- `apps/dsa-web/src/api/analysis.ts`

目标：理解原项目如何从前端点击分析，到后端创建任务，再到报告返回。

### 阶段 2：搭建自己的后端

目标：

- FastAPI 能启动。
- `/api/health` 能访问。
- 自选股接口可用。
- SQLite 能保存数据。

当前已完成：

- FastAPI 最小后端；
- 健康检查接口；
- 自选股接口；
- 自选股 SQLite 持久化；
- mock 分析任务接口。

### 阶段 3：实现分析任务闭环

目标：

- 创建任务。
- 查询任务状态。
- mock 生成报告。
- 保存报告。
- 查询历史报告。

当前已完成：

- mock 分析任务创建；
- mock 分析任务查询；
- mock 报告生成；
- 报告详情查询；
- 历史报告列表查询。

### 阶段 4：搭建手机 APP

目标：

- 创建 5 个页面。
- 设置页保存后端地址。
- 首页能显示自选股。
- 点击分析进入任务页。
- 任务完成后进入报告详情。
- 历史页能查看报告。

当前已完成（移动端第一阶段）：

- 创建 Expo + React Native + TypeScript 项目骨架。
- 使用 Expo Web 预览。
- 设置页：输入后端地址、保存到 AsyncStorage、测试 `/api/health`。
- 后端增加 CORS 配置。
- 首页：读取 `/api/stocks`、FlatList 展示、加载/失败/空列表状态。
- 引入 React Navigation 底部 Tab 导航。
- 页面拆分为独立 Screen。
- `App.tsx` 简化为 APP 入口。

当前移动端结构：

```text
mobile-app/
  App.tsx
  src/
    navigation/
      AppNavigator.tsx
    screens/
      HomeScreen.tsx
      SettingsScreen.tsx
```

长期移动端导航规划：

```text
底部 Tab = 一级模块
Stack 页面 = 模块内部流程页
```

长期产品定位：

```text
AI 股票分析助手，不是投资交易软件。
核心能力：自选股票 + AI 问答 + 历史分析报告 + 个人账号。
```

长期底部 Tab 方向：

```text
自选 | 问股 | 报告 | 我的
```

各模块职责：

| 模块 | 职责 |
|---|---|
| 自选 | 自选股列表、添加/删除股票、触发分析、进入任务状态和报告详情 |
| 问股 | 输入问题或股票代码，进行 AI 问答和个股咨询；后续可扩展“问大盘” |
| 报告 | 历史分析报告列表，点击进入报告详情；后续可扩展大盘复盘报告 |
| 我的 | 承接当前设置页能力，包括后端地址、连接测试、用户偏好配置；后续扩展登录、注册、账号信息 |

说明：

```text
大盘短期不作为底部 Tab。
后续如果大盘能力变重，可以放入问股或报告模块。
```

自选模块内部页面规划：

```text
WatchlistScreen       # 自选股列表
TaskStatusScreen      # 分析任务状态
ReportDetailScreen    # 报告详情
```

说明：

```text
任务状态页和报告详情页不作为底部 Tab，
它们更适合作为“自选模块”内部的二级页面。
```

移动端第二阶段：完善自选模块。

当前已完成并通过手动验收：

1. 将 `HomeScreen` 重命名为 `WatchlistScreen`。
2. 自选页实现添加股票，调用 `POST /api/stocks`。
3. 自选页实现删除股票，调用 `DELETE /api/stocks/{code}`。
4. 点击股票创建分析任务，调用 `POST /api/analysis`。
5. 自选页初步显示每只股票最近一次任务状态。
6. 新增 `TaskStatusScreen`，用于查看分析任务状态。
7. 新增 `ReportDetailScreen`，用于查看报告详情。
8. 底部 Tab 调整为：自选、问股、大盘、设置。
9. 自选模块内部使用 Stack 导航：`Watchlist -> TaskStatus -> ReportDetail`。

验收结果：

```text
添加自选股通过
删除自选股通过
点击股票创建分析任务通过
任务状态页通过
报告详情页通过
Tab + Stack 导航结构通过
```

当前说明：

```text
分析任务和报告内容仍然是 mock 数据。
下一阶段暂不急于推进真实行情，先完成本阶段记录和代码提交。
```

### 阶段 5：接入真实能力

第一版闭环完成后，再逐步学习并接入真实能力。

当前进展：

```text
第三阶段已完成：真实行情最小接入
第四阶段已完成：行情基础能力增强
第五阶段已完成：历史报告列表 + 新导航结构
```

已完成内容：

1. 新增 `GET /api/market/quote/{code}` 行情测试接口。
2. 新增 `market_data.py` 服务层，通过 `efinance` 获取 A 股基础行情。
3. `POST /api/analysis` 已使用真实股票名称、当前价、涨跌幅生成报告。
4. 移动端报告详情页已展示涨跌幅、数据来源、行情获取时间。
5. 已验证 `600519` 能返回真实行情并生成报告。
6. 已支持 A 股常见代码格式标准化，例如 `600519.SH`、`SH600519`、`SZ000001`。
7. 已增加 60 秒行情内存缓存，减少重复请求第三方行情源。
8. 已增加前端添加自选股时的股票代码基础校验。
9. 已增加行情错误分类，区分代码错误、行情源网络错误、空数据、股票未找到、字段为空和字段格式异常。
10. 底部 Tab 调整为：自选、问股、报告、我的。
11. 新增报告模块 Stack：`ReportHistory -> ReportDetail`。
12. 新增历史报告列表页，调用 `GET /api/reports`。
13. 历史报告列表支持加载中、失败、空列表和成功列表状态。
14. 点击历史报告可进入报告详情页。
15. `ReportDetailScreen` 已支持从自选模块和报告模块复用。
16. “我的”Tab 暂时复用设置页，保留后端地址配置和连接测试。
17. v0.2 阶段 1 已完成：MA5 / MA10 / MA20、乖离率、动态评分、动态建议和报告详情增强。
18. v0.2 阶段 2 已完成：历史报告列表增强，列表展示价格、涨跌幅和趋势摘要，并兼容旧报告缺失字段。
19. v0.2 阶段 3 已完成：fallback 最小版，明确 `efinance` 为主行情源，拆分主数据源函数，预留备用数据源入口，并增强行情失败提示。
20. 学习中发现的问题已记录到 `docs/code-review-notes.md`。

后续继续学习方向：

1. 问股最小版。
2. 我的页基础整理。
3. 真实备用数据源接入。
4. MACD / RSI。
5. AI 总结报告。
6. Markdown 报告。
7. 大盘复盘。
8. 登录 / 注册 / 个人账号体系。
9. 推送提醒。

## 9. 和 AI 助手协作方式

为了更好学习，建议每次只做一个小目标。

常用表达：

| 场景 | 推荐说法 |
|---|---|
| 想了解代码 | 进入研究模式，帮我解释这个文件 |
| 想比较方案 | 进入创新模式，比较这几个方案 |
| 想开始设计 | 进入计划模式，帮我制定详细计划 |
| 想写代码 | 进入执行模式，按刚才计划实现 |
| 想检查结果 | 进入审查模式，检查是否符合计划 |
| 不知道下一步 | 当前做到这里了，下一步建议做什么 |

协作原则：

- 不急着写代码，先搞清楚目标。
- 涉及代码变更前先计划。
- 每次只做一个可验证的小任务。
- 写完代码要运行或检查。
- 文档只有明确允许时才生成或更新。

## 10. 项目管理学习点

后续需要逐步学习：

| 能力 | 学习内容 |
|---|---|
| Git | 初始化、提交、分支、远程仓库、回滚 |
| 版本规划 | v0.1、v0.2、v1.0 的功能范围 |
| Issue 管理 | 把大功能拆成小任务 |
| README | 记录项目定位、启动方式、接口说明 |
| CHANGELOG | 记录每次版本变化 |
| 测试 | 后端接口测试、前端页面测试 |
| 复盘 | 每个阶段总结学到了什么 |

## 11. 第一版验收标准

第一版完成时，应能走通：

```text
打开 APP
  ↓
设置后端地址
  ↓
添加股票 600519
  ↓
点击分析
  ↓
看到任务状态变化
  ↓
看到报告详情
  ↓
返回历史页能看到刚才的报告
```

能完成这个流程，就说明第一版最小闭环成功。

## 12. 当前下一步

当前已创建：

```text
ai-stock-app/backend
ai-stock-app/mobile-app
ai-stock-app/docs
ai-stock-app/docs/releases/v0.1-summary.md
```

当前状态：

1. v0.1 AI Stock App MVP 已完成并通过手动验收。
2. v0.1 总结文档已生成：`docs/releases/v0.1-summary.md`。
3. v0.2 阶段 1 已完成：MA 指标和动态报告。
4. v0.2 阶段 2 已完成：历史报告列表增强。
5. v0.2 阶段 3 已完成：fallback 最小版。
6. 本地阶段成果已推送到远程仓库。

建议下一步：

1. 继续 v0.2 阶段 4：问股最小版。
2. 实现问股 Tab 输入股票代码并返回基础分析。
3. 继续保持小步闭环：先计划、再执行、学习、记录问题、审查。
